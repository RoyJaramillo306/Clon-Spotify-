import { ImgFailDirective } from './img-fail.directive';

describe('ImgFailDirective', () => {
  it('should create an instance', () => {
    const directive = new ImgFailDirective();
    expect(directive).toBeTruthy();
  });
});
